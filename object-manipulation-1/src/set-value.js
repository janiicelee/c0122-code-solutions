/* exported setValue */
/*

- input: object, key, value
- output: undefined

- add the {key: value} pair in the right position of the object.

*/

function setValue(object, key, value) {
  object[key] = value;
}
