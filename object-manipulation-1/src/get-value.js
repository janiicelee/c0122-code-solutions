/* exported getValue */

/*

- input: object and key
- output: value

- retrieve the value that is according to the right key in the object

*/

function getValue(object, key) {
  return object[key];
}
